import React from 'react';
import UploadForm from '../components/UploadForm/UploadForm';

const Upload = () => {
  return (
    <div>
      Upload
      <UploadForm />
    </div>
  );
};

export default Upload;
